# Copyright (c) 2026 Abetworks (abetworks.in). All rights reserved.
# Abetworks Proprietary and Confidential.
#
# Self-contained stack: Postgres + order-svc. Run from the REPO ROOT:
#   docker compose -f niche-plans/08-abetfield/scaffolding/services/order-svc/docker-compose.yaml up --build
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: abetfield
      POSTGRES_HOST_AUTH_METHOD: trust
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 20

  order-svc:
    build:
      context: ../../../../..
      dockerfile: niche-plans/08-abetfield/scaffolding/services/order-svc/Dockerfile
    environment:
      DATABASE_URL: postgres://postgres:dev@postgres:5432/abetfield
      JWT_SECRET: dev-secret-change-me
      PORT: "3013"
    ports: ["3013:3013"]
    depends_on:
      postgres:
        condition: service_healthy
